//- Copyright (c) Microsoft. All rights reserved. Licensed under the MIT license.
//- See LICENSE in the project root for license information.

extends layout

block content

  .ms-CommandBar
    .ms-CommandBar-items
      a.ms-Link(href='/') Home
    .ms-CommandBar-items.ms-CommandBar-sideCommands
      .ms-font-s #{current.microsoftAccountName}
  .ms-Grid        
    .ms-Grid-row
      .ms-NavBar
        .ms-NavBar-items
          .ms-font-xl.ms-fontColor-themePrimary #{current.displayName}'s journal
          .ms-font-l Your recent notes
          .ms-font-m            
              .ms-Grid
                  .ms-Grid-row
                      a.ms-Link(href='/graph/getMyPages') Get my pages
                  if response
                    .ms-font-m #{response}

    //- <nav class="navbar navbar-default navbar-fixed-top navbar-inverse" ng-controller="NavbarController as nav">
    //-   <div class="container-fluid">
    //-     <div class="navbar-header">
    //-       <button type="button" class="navbar-toggle collapsed" ng-click="nav.isCollapsed = !nav.isCollapsed">
    //-         <span class="icon-bar"></span>
    //-         <span class="icon-bar"></span>
    //-         <span class="icon-bar"></span>
    //-       </button>
    //-       <a class="navbar-brand" href="#/">Microsoft Graph Snippets</a>
    //-     </div>
    //-     <div class="navbar-collapse" collapse="nav.isCollapsed">
    //-       <ul class="nav navbar-nav navbar-right">
    //-         <li ng-show="!nav.isConnected" ng-click="nav.connect()" class="navbar-signin">
    //-           <a>Connect</a>
    //-         </li>
    //-         <li ng-show="nav.isConnected" ng-click="nav.disconnect()" class="navbar-signin">
    //-           <a>Disconnect</a>
    //-         </li>
    //-       </ul>
    //-     </div>
    //-   </div>
    //- </nav>